<network-security-config>
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added CAs (e.g. Charles) while debuggable only -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
